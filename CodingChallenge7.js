function whosPaying(names) {
    
    /******Don't change the code above*******/
        
        //Write your code here.
        var randomSelector = Math.floor(Math.random() * names.length);
        return names[randomSelector] + " is going to buy lunch today!";
        
    
    /******Don't change the code below*******/    
    }